#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: integer 1
 * @b: integer 2
 *
 * Return: always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	printf("%d", mul);

	return (0);
}
